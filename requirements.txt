

# Core API Framework (REQUIRED)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Document Processing (REQUIRED)
PyMuPDF==1.23.8              # Used for PDF parsing
pdfplumber==0.10.3           # Used for PDF parsing and table extraction
requests==2.32.3             # Used for downloading PDFs

# OpenAI Integration (REQUIRED)
openai>=1.3.0                # Used for GPT-4o

# Semantic Search (REQUIRED)
sentence-transformers==2.2.2 # Used for semantic embeddings
torch>=2.0.0                 # Required by sentence-transformers
scikit-learn==1.3.2          # Used for cosine_similarity and TfidfVectorizer
numpy==1.24.3                # Used for array operations

# Caching (REQUIRED)
cachetools==5.3.2            # Used for TTLCache

# Environment (REQUIRED)
python-dotenv==1.0.0         # Used for loading .env file

